interface StickyBookingButtonProps {
  onNavigate?: (page: string) => void;
}

/** Sticky CTA removed on mobile per design. Can re-enable or show on desktop if needed. */
export default function StickyBookingButton(_props: StickyBookingButtonProps) {
  return null;
}

